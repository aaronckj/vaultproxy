# Copy to wg.env and populate from OPNsense (see ../audit-egress-README.md).
# wg.env should be chmod 600 and is gitignored.

WIREGUARD_PRIVATE_KEY=
WIREGUARD_PUBLIC_KEY=
WIREGUARD_PRESHARED_KEY=
WIREGUARD_ENDPOINT_IP=
WIREGUARD_ENDPOINT_PORT=51820
WIREGUARD_ADDRESSES=
